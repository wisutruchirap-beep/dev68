apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # Key-value pairs
  database_url: "postgres://localhost/mydb"
  cache_size: "100"
  
  # File content
  app.properties: |
    server.port=8080
    server.timeout=30
    logging.level=INFO