apiVersion: v1
kind: Service
metadata:
  name: web-service
spec:
  type: ClusterIP      # Internal only
  selector:
    app: web  	       # Select pods with this label
  ports:
  - port: 80           # Service port
    targetPort: 80
    protocol: TCP